<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Основная тема приложения для API 29-30 -->
    <style name="Theme.OxideLabMobile" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/background_gradient</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        
        <!-- Splash screen настройки доступны только для API 31+ -->
        <!-- Они определены в values-v31/themes.xml -->
    </style>
</resources>